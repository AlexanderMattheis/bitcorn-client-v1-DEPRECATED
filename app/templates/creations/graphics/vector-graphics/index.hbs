{{#files-lister
    page=page
    setPage=(action "setPage")
    filter=(action "filterByTag") as |data|}}

  {{#each data as |listing|}}
    {{file-listing linking=false model=listing}}
  {{/each}}

{{/files-lister}}