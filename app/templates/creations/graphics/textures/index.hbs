{{#files-lister
    page=page
    setPage=(action "setPage")
    filter=(action "filterByTag") as |data|}}

  {{#each data as |listing|}}
    {{file-listing
        data=listing linking=true
        routeName="creations.graphics.textures.show" model=listing}}
  {{/each}}

{{/files-lister}}
